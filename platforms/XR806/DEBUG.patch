index 1ea9c93..000a0df 100644
--- a/src/image/image_debug.h
+++ b/src/image/image_debug.h
@@ -37,18 +37,18 @@
 extern "C" {
 #endif
 
-#define IMAGE_DBG_ON    0
-#define IMAGE_WRN_ON    0
+#define IMAGE_DBG_ON    1
+#define IMAGE_WRN_ON    1
 #define IMAGE_ERR_ON    1
 #define IMAGE_ABORT_ON  0
 
-#define FDCM_DBG_ON     0
-#define FDCM_WRN_ON     0
+#define FDCM_DBG_ON     1
+#define FDCM_WRN_ON     1
 #define FDCM_ERR_ON     1
 #define FDCM_ABORT_ON   0
 
-#define FLASH_DBG_ON    0
-#define FLASH_WRN_ON    0
+#define FLASH_DBG_ON    1
+#define FLASH_WRN_ON    1
 #define FLASH_ERR_ON    1
 #define FLASH_ABORT_ON  0
 
diff --git a/src/rom/rom_bin/src/image/image.c b/src/rom/rom_bin/src/image/image.c
index b2f13b3..09fe2f6 100644
--- a/src/rom/rom_bin/src/image/image.c
+++ b/src/rom/rom_bin/src/image/image.c
@@ -472,6 +472,10 @@ uint32_t tzboot_image_get_addr(image_priv_t *img, image_seq_t seq, uint32_t id)
  */
 uint32_t image_get_section_addr(uint32_t id)
 {
+	image_set_dbg_mask(IMAGE_ERR_FLAG | FDCM_ERR_FLAG | FLASH_ERR_FLAG |
+			   IMAGE_WRN_FLAG | FDCM_WRN_FLAG | FLASH_WRN_FLAG |
+			   IMAGE_DBG_FLAG | FDCM_DBG_FLAG | FLASH_DBG_FLAG);
+
 	IMAGE_DBG("%s(), seq %u, id %#x\n", __func__, image_priv.iop.running_seq, id);
 	return image_get_addr(&image_priv, image_priv.iop.running_seq, id);
 }
